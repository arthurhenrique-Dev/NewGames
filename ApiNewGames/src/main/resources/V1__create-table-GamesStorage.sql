CREATE TABLE games_storage (
                               id INT AUTO_INCREMENT PRIMARY KEY,
                               genre VARCHAR(100),
                               name VARCHAR(255),
                               price DECIMAL(10, 2),
                               unit INT
);
